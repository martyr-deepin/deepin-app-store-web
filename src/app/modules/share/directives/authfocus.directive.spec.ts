import { AuthfocusDirective } from './authfocus.directive';

describe('AuthfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthfocusDirective();
    expect(directive).toBeTruthy();
  });
});
